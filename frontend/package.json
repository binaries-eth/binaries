{
  "name": "binaries",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "ghpages": "(npm run build; cd dist; git add -A; git commit -m 'deploy'; git push)"
  },
  "dependencies": {
    "ethers": "^5.4.7",
    "vue": "^3.2.16"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.9.3",
    "autoprefixer": "^10.3.7",
    "postcss": "^8.3.9",
    "typescript": "^4.4.3",
    "vite": "^2.6.4",
    "vue-tsc": "^0.3.0",
    "@ibm/plex": "^6.0.0"
  }
}
